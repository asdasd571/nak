#include <bits/stdc++.h>
#define fast cin.tie(0),cout.tie(0),ios::sync_with_stdio(0)
using namespace std;
int k,cnt;
int num[10];
int num1[10];
string a;

int main(void){
    cin >> k;
    for(int i=0; i<10; i++){
        num[i] = i;
    }
    for(int i=0; i<k; i++){
        cin >> a[i];
        if(a[])
    }
}

//처음에 나올 수 있는 max 수 9,8 -> 
//처음에 나올 수 있는 min 수 1,0 ->